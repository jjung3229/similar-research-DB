# 핵심 기능은 파이썬 표준 라이브러리만으로 동작한다. (폐쇄망 고려)
# 아래는 전부 선택 사항이다.

PyYAML>=6.0                              # config.yaml 사용 시. 없으면 config.json 사용
pywin32>=306 ; sys_platform == "win32"   # Outlook 메일·일정 수집 시. 없으면 메일만 제외됨
